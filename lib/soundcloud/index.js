const download = require('./download');
const getConfig = require('./getConfig');
const resolveUrl = require('./resolveUrl');

/** @module SoundCloud */
module.exports = {
  download,
  getConfig,
  resolveUrl,
};

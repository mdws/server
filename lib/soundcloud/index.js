const download = require('./download');
const getInfo = require('./getInfo');
const resolveUrl = require('./resolveUrl');

/** @module SoundCloud */
module.exports = {
  download,
  getInfo,
  resolveUrl,
};
